class Solution:
    def reverse(self, x: int) -> int:
        s = str(abs(x))
        if int(s[::-1]) >= 2147483648:
            return 0
        else:
            if x>0:
                return int(s[::-1])
            else:
                return -int(s[::-1])
