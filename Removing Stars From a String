class Solution:
    def removeStars(self, s: str) -> str:
        l= []
        for i in range(len(s)):
            if s[i]!='*':
                l.append(s[i])
            else:
                l.pop()
        return "".join(l)
